<%= form_for(@pat, remote: true) do |f| %>
  <% if @pat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pat.errors.count, "error") %> prohibited this pat from being saved:</h2>

      <ul>
      <% @pat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :firstname %><br>
    <%= f.text_field :firstname %>
  </div>
  <div class="field">
    <%= f.label :lastname %><br>
    <%= f.text_field :lastname %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div>
    <%= select_tag(:ward_id, grouped_options_for_select(@grouped_options), prompt: 'Choose Ward') %>
  </div> 

  <div class="actions">
  <input name="commit" type="submit" value="Create Pat" class="button tiny radius"  />
  

    <%#= f.submit %>
  </div>
<% end %>





